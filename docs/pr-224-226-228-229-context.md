# Context Notes — PR covering #224, #226, #228, #229

> Working notes only. Not real repo state — written to compare against
> the actual PR/diff once it exists.

## Repo assumptions
- Repo: ApexChainx/ApexChainx-Contracts — same Rust/Soroban crate
  (`apexchainx_calculator`) as the prior batch (#233/#230/#234/#236),
  different assignee (jerrydanzaria5-svg vs. cokehill).
- Same shared issue template across all four (Problem Statement / Why
  it matters / Technical Context / Expected Outcome / Acceptance
  Criteria) — acceptance criteria again truncated in screenshots after
  the first 2 bullets ("Scope is intentionally small..." / "preserves
  no_std, Soroban, CI conventions").
- Public contract behavior shaped by config, history, governance,
  pause, telemetry, version negotiation, and event schema modules
  (per shared boilerplate) — exact module boundaries not confirmed.

## Issue #224 — Contract complexity budget for public API
- Assumed "complexity budget" means some enforced check (CI lint,
  script, or code review checklist) rather than a purely social
  convention — issue doesn't specify mechanism.
- Unknown: what counts as "the public API surface" precisely (all
  `pub fn` in `lib.rs`? Only contract entrypoints exposed via Soroban
  invocation?) and what threshold/metric defines the "budget."

## Issue #226 — Document backend bridging assumptions
- Assumed `get_failure_schema` and `get_version_info` are existing
  public contract entrypoints (not confirmed beyond the issue title).
- Labeled **breaking** in the actual issue — meaning this doc work may
  reveal or necessitate a breaking change, not just clarify existing
  behavior. I don't know what the "breaking" concern actually is.
- Unknown: what "backend bridging" refers to here — a separate
  off-chain backend service that calls these entrypoints? An indexer?
  Not specified.

## Issue #228 — Formal patchability audit
- Assumed "patchability" means: can storage layout or event schemas
  be changed in a future upgrade without breaking existing on-chain
  history or off-chain consumers reading past events.
- Unknown: whether this contract has any existing migration/upgrade
  mechanism already, which would materially change what the audit
  needs to cover.

## Issue #229 — Contributor workflow for Soroban XDR examples
- Assumed this is tooling (a script) + documentation, not a change to
  contract logic itself.
- Unknown: whether the repo already has any example-generation
  tooling to extend, or whether this is built from scratch, and which
  public contract calls need example coverage.

## Biggest risk to the guessed PR diverging from reality
1. Issue #226 is flagged "breaking" — the real PR likely involves an
   actual behavior/API change, not just documentation, which this
   guess doesn't capture.
2. Issue #224's enforcement mechanism (CI gate vs. manual review vs.
   test-count heuristic) is a complete guess.
3. Issue #228 depends heavily on whether an upgrade/migration system
   already exists — unknown.

## To fill in once you have real repo access
- [ ] Read full Acceptance Criteria for all four issues (screenshots were cut off)
- [ ] Understand what "breaking" means for #226 — is this a doc-only PR or does it require an API change?
- [ ] Confirm existing (if any) upgrade/migration mechanism relevant to #228
- [ ] Confirm existing example-generation tooling, if any, relevant to #229
- [ ] Define concrete metric/mechanism for the "complexity budget" in #224