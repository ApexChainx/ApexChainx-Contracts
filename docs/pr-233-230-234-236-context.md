# Context Notes — PR covering #233, #230, #234, #236

> Working notes only. Not real repo state — written to compare against
> the actual PR/diff once it exists.

## Repo assumptions
- Repo: ApexChainx/ApexChainx-Contracts — a Rust/Soroban smart
  contract crate centered on `apexchainx_calculator`.
- Architecture is documented in `README.md` and `docs/PROJECT_CONTEXT.md`
  (per issue text); active contract implementation lives under
  `apexchainx_calculator/src/lib.rs`.
- Public contract behavior is shaped by: config, history, governance,
  pause, telemetry, version negotiation, and event schema modules
  (per issue text) — exact file/module boundaries not confirmed.
- All four issues use an identical template (Problem Statement / Why
  it matters / Technical Context / Expected Outcome / Acceptance
  Criteria), and all four Acceptance Criteria sections were truncated
  in the screenshots after "Scope is intentionally small and
  reviewable" + "preserves no_std, Soroban, and CI conventions" — the
  remaining criteria lines weren't visible, so I don't have the full
  acceptance list for any of the four issues.

## Issue #233 — Config-freeze regression test
- Assumed there's an existing "config-freeze" mechanism that can be
  triggered and an existing "history" module storing entries that can
  be "in-flight" (not yet finalized) at the time of freeze.
- Unknown: exact API for triggering freeze, and what "in-flight"
  precisely means in this contract's history model (pending vs.
  unconfirmed vs. mid-transaction).

## Issue #230 — Audit testnet deploy workflow
- Assumed there's an existing CI/CD workflow file (likely GitHub
  Actions) that handles testnet deploys with build, lint, and deploy
  steps already in place (per issue's "CI workflows that already
  exercise build, lint, and deployment surfaces").
- Unknown: current naming scheme for deploy artifacts, current
  retention policy/settings, and what specifically is non-deterministic
  today (timestamps in filenames? unpinned dependency versions?).

## Issue #234 — Document MAX_REASON_LEN / pause-state guardrails
- Assumed `MAX_REASON_LEN` is an existing constant bounding some
  string/reason field length (e.g., a governance action reason or
  pause reason).
- Unknown: the actual numeric value, where it's enforced (contract
  entrypoint validation vs. storage layer), and the precise
  interaction with pause-state (does exceeding it block a pause action?
  does pause state change what's validated?).

## Issue #236 — Module maturity maintenance note
- Assumed this is purely a documentation change (likely an addition
  to `docs/PROJECT_CONTEXT.md` or a new `docs/MODULE_STATUS.md`).
- Unknown: which modules the maintainer actually considers
  feature-complete vs. experimental — this is a judgment call only
  the maintainer/team can make; I have no basis to guess correctly
  here beyond listing the modules named in the issue template itself
  (config, history, governance, pause, telemetry, version negotiation,
  event schema).

## Biggest risk to the guessed PR diverging from reality
1. Issue #236 in particular can't be meaningfully guessed — the
   feature-complete/experimental split is a real judgment call, not
   inferable from the issue text.
2. Truncated Acceptance Criteria on all four issues — real PRs may
   need to satisfy criteria I never saw (the screenshots cut off
   after 2 bullet points on each).
3. Exact deploy workflow file location/name is unknown, so the #230
   diff's file path is a pure guess.

## To fill in once you have real repo access
- [ ] Read full Acceptance Criteria for all four issues (screenshots were cut off)
- [ ] Confirm existing config-freeze / history module APIs (#233)
- [ ] Confirm current CI/CD workflow file path and existing naming/retention config (#230)
- [ ] Confirm actual MAX_REASON_LEN value and enforcement location (#234)
- [ ] Get maintainer input on which modules are actually feature-complete vs. experimental (#236)