# Context Notes — PR covering #238, #235, #232, #231

> Working notes only. Not real repo state — written to compare against
> the actual PR/diff once it exists.

## Repo assumptions
- Repo: ApexChainx/ApexChainx-Contracts — same Rust/Soroban crate
  (`apexchainx_calculator`) as the two prior batches. This batch is
  assigned to gloriaelishahabu (different from cokehill and
  jerrydanzaria5-svg on earlier batches).
- Same shared issue template across all four; acceptance criteria
  truncated in every screenshot after the standard first two bullets.
- Public contract behavior shaped by config, history, governance,
  pause, telemetry, version negotiation, and event schema modules
  (per shared boilerplate) — exact module boundaries not confirmed.
- Note: #231 ("breaking change" definitions) and #226 from an earlier
  batch (labeled "breaking") are likely related — the breaking-change
  definition work in this PR may retroactively clarify or reshape
  what #226 was actually asking for. Not confirmed without reading
  both issues' full text side by side.

## Issue #238 — Config snapshot/hash stability under no-op writes
- Assumed `get_config_snapshot` and `get_config_version_hash` are
  existing public read entrypoints, and that "no-op config write"
  means submitting a config-write transaction whose payload matches
  current config exactly.
- Unknown: whether the version hash is expected to change on *any*
  write attempt (even no-ops) in current behavior, or whether this
  is confirming/fixing a regression already suspected to exist.

## Issue #235 — Codeowner/review policy for security-sensitive modules
- Assumed implementation is a `.github/CODEOWNERS` file and/or branch
  protection rule changes, not a code change.
- Unknown: which specific modules/paths are considered
  "security-sensitive" beyond the general list in the shared
  boilerplate (config, governance, pause) — and who the actual
  codeowners/reviewers should be (org-specific, can't guess).
- Unknown: what "release workflows" refers to concretely — likely
  the same CI/deploy workflow audited in issue #230 from an earlier
  batch, but not confirmed.

## Issue #232 — Canonical explanation of admin/operator auth vs. public read paths
- Assumed there are two distinct role concepts (admin, operator) with
  different auth scopes, and some public contract methods are reads
  that may or may not require either role.
- Unknown: the actual current behavior — whether any reads are
  currently gated at all, or whether this issue is purely defensive
  documentation of behavior that's already fully open.

## Issue #231 — Define what counts as a breaking change
- Assumed scope is limited to event schemas, contract state layout,
  and storage interfaces (per title) — not the full public API
  surface (which #224 already covers separately as a complexity
  budget).
- Unknown: whether this repo has any existing versioning/semver-like
  convention already in place that this issue is meant to formalize
  vs. invent from scratch.

## Biggest risk to the guessed PR diverging from reality
1. #235's actual codeowner list/module boundaries are an org decision
   I have no way to guess correctly.
2. #238 might be confirming a known bug (hash *does* change on no-op
   writes today) rather than adding a test for already-correct
   behavior — direction of the fix is unclear from title alone.
3. #231 and #226 (earlier batch, labeled "breaking") may need to be
   read together — this PR's definitions could directly resolve or
   reshape the scope of that earlier issue.

## To fill in once you have real repo access
- [ ] Read full Acceptance Criteria for all four issues (screenshots were cut off)
- [ ] Confirm current get_config_snapshot / get_config_version_hash behavior on no-op writes (bug vs. spec) (#238)
- [ ] Get maintainer input on actual codeowners and security-sensitive module list (#235)
- [ ] Confirm current auth gating (if any) on public read paths (#232)
- [ ] Cross-reference #231 with #226 to see if they're meant to be resolved together